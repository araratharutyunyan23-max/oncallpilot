"""Evals-as-CI-gate (Phase 3): golden datasets + graders + a baseline-ratcheted
gate. Deterministic checks (retrieval, tool selection, confirmation gate) block
without judgment; faithfulness is scored by an LLM judge (structured outputs)."""
